language: node_js

node_js:
  - "8"

services:
  - docker

script:
  # docker login
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  # build, create docker image and upload to dockerhub
  - make release